# Project Memory — Greenlight

Decisions, context, and non-obvious state. Update as the project evolves.

## Theme type
Classic PHP theme (no FSE/block theme). If migration to block theme, update CLAUDE.md accordingly.

## Design decisions
- No dark mode — palette is light-only, intentional
- Josh Comeau CSS reset inlined in `<head>` (not via `wp_enqueue_style`)
- No JavaScript at all currently — layout is 100% CSS

## Environment
- Local dev : (à documenter — ex. http://greenlight.local)
- WordPress version : (à documenter)
- PHP version : (à documenter)

## Active constraints
- No SEO plugin — theme handles canonical + OG meta itself
- No page builder — templates are hand-written PHP

## Notes
(Ajouter ici les décisions, découvertes et contexte non évident au fil du projet)
